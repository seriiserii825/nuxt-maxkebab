export interface CheckboxOption {
  label: string;
  price: number;
  checked: boolean;
  group: string;
}

export interface StepperOption {
  label: string;
  price: number;
  count: number;
  group: string;
}
